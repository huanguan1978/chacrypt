name: chabox
description: "Secure, open-source, offline file enc/decryption. Uses stream-based processing for high performance and integrity—no compression, no data manipulation, ensuring side-channel safety."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  device_info_plus: ^13.1.0
  package_info_plus: ^10.1.0
  logging: ^1.3.0
  window_manager: ^0.5.1

  chapose:
    path: ../chapose
  # basic_message:
  #   path: ../../messages/packages/basic_message
  # app_lifecycle_protector:
  #   path: ../../avatar/app_lifecycle_protector

  basic_logger: ^0.2.2
  basic_message: ^1.0.1
  app_lifecycle_protector: ^0.0.1  

  shared_preferences: ^2.5.5
  file_selector: ^1.1.0
  dart_eval: ^0.8.4
  path_provider: ^2.1.5
  path: ^1.9.1
  desktop_drop: ^0.7.0
  mime: ^2.0.0
  tar: ^2.0.2
  glob: ^2.1.3
  flutter_markdown_plus: ^1.0.7
  file: ^7.0.1
  font_awesome_flutter: ^11.0.0
  url_launcher: ^6.3.2
  no_screenshot: ^1.1.0
  local_auth: ^3.0.1
  flutter_it: ^2.1.2
  yaml: ^3.1.3
  share_plus: ^13.1.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  intl_translation: ^0.21.0
  package_rename: ^1.10.1
  flutter_launcher_icons: ^0.14.4

# For inform  ation on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Attempted to generate localizations code without having the flutter: generate flag turned on.
  generate: true
  
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/eula/
    - assets/license/
    - assets/security/
    - assets/unstaged/
    - assets/ecos/
    - assets/
    
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# dart run package_rename
package_rename_config:
  macos:
    app_name: ChaBox
    package_name: com.iche2.chabox.macos
    copyright_notice: © 2026 Heshan Aisipai E-commerce Co., Ltd. All rights reserved.
  # ios:
  #   app_name: ChaBox
  #   bundle_name: ChaBox
  #   package_name: com.iche2.chabox.ios
  # android:
  #  app_name: ChaBox
  #  package_name: com.iche2.chabox.android
  #  override_old_package: com.example.chabox
    # lang: kotlin

  # web:
  #   app_name: ChaBox
  #   description: Offline Secure Vault.

  # linux:
  #   app_name: ChaBox
  #   package_name: com.iche2.chabox.linux
  #   exe_name: chabox

  # windows:
  #   app_name: ChaBox
  #   organization: Heshan Aisipai E-commerce Co., Ltd.
  #   copyright_notice: © 2026 Heshan Aisipai E-commerce Co., Ltd. All rights reserved.
  #   exe_name: chabox

# dart run msix:create
msix_config:
  display_name: ChaBox
  publisher_display_name: crown.hg
  identity_name: crown.hg.ChaBox
  publisher: CN=8D803B40-1C19-4FC9-8CF8-DC52875FAEF3
  store: true
  enable_at_startup: false  
  logo_path: D:\MyProjects\avatar\chacrypt\chabox\assets\gai_icon_square_512x512.png
  languages: en-us, zh-cn
  # capabilities: internetClient
  msix_version: 1.0.0.0
  output_path: D:\MyProjects\ChaBoxSetup
  output_name: ChaBox-1.0.0-Setup
  windows_build_args: --obfuscate --split-debug-info=D:\MyProjects\avatar\chacrypt\chabox\build\windows\x64\runner\chabox-symbols
  execution_alias: chabox

# dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: "assets/chabox_icon_square_512x512.png"
  android: false # "launcher_icon"
  min_sdk_android: 27
  image_path_android : "assets/chabox_icon_square_512x512.png"  
  ios: false
  image_path_ios : "assets/chabox_icon_rounded_512x512.png"
  remove_alpha_ios: true
  macos:
    generate: false
    image_path: "assets/chabox_icon_rounded_512x512.png"
  windows:
    generate: false
    image_path: "assets/chabox_icon_square_512x512.png"
    icon_size: 48 # min:48, max:256, default: 48
  # web:
  #   generate: true
  #   image_path: "path/to/image.png"
  #   background_color: "#hexcode"
  #   theme_color: "#hexcode"